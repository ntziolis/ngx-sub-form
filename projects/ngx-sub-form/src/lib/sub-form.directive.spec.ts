import { SubFormDirective } from './sub-form.directive';

describe('SubFormDirective', () => {
  it('should create an instance', () => {
    const directive = new SubFormDirective();
    expect(directive).toBeTruthy();
  });
});
